#pragma once
#include <set>
#include <cmath>
#include <array>
#include <vector>
#include <iostream>

using std::set;
using std::array;
using std::vector;

using std::swap;
using std::endl;

using std::cout;

struct TreeNode
{
    int val;
    vector<TreeNode*> children;
};
